rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Default deny catch-all
    match /{document=**} {
      allow read, write: if false;
    }

    // Helper functions
    function isSignedIn() {
      return request.auth != null;
    }

    function isAdmin() {
      return (request.auth != null && request.auth.token != null && (request.auth.token.email == "kawanyuri35@gmail.com" || request.auth.token.email == "eukoosh@gmail.com"))
             || (request.resource != null && request.resource.data != null && request.resource.data.admin_secret == "pkxd2026_super_secret_admin_key");
    }

    function isDeletable() {
      return (request.auth != null && request.auth.token != null && (request.auth.token.email == "kawanyuri35@gmail.com" || request.auth.token.email == "eukoosh@gmail.com"))
             || (resource != null && resource.data != null && resource.data.admin_secret == "pkxd2026_super_secret_admin_key");
    }

    // News rules
    match /news/{newsId} {
      allow read: if true;
      allow create, update: if isAdmin();
      allow delete: if isDeletable();
    }

    // AppSettings rules
    match /settings/{settingsId} {
      allow read: if true;
      allow create, update: if isAdmin();
      allow delete: if isDeletable();
    }

    // PastSpoiler rules
    match /past_spoilers/{spoilerId} {
      allow read: if true;
      allow create, update: if isAdmin();
      allow delete: if isDeletable();
    }

    // Featured Videos rules
    match /featured_videos/{videoId} {
      allow read: if true;
      allow create, update: if isAdmin();
      allow delete: if isDeletable();
    }

    // Theories rules
    match /theories/{theoryId} {
      allow read: if true;
      allow create: if isAdmin();
      allow update: if isAdmin() || (incoming().diff(existing()).affectedKeys().hasOnly(['likes']) && incoming().likes == existing().likes + 1);
      allow delete: if isDeletable();
    }

    function incoming() {
      return request.resource.data;
    }

    function existing() {
      return resource.data;
    }

    // Shorts rules
    match /shorts/{shortId} {
      allow read: if true;
      allow create, update: if isAdmin();
      allow delete: if isDeletable();
    }

    // Notifications rules
    match /notifications/{notifId} {
      allow read: if true;
      allow create, update: if isAdmin();
      allow delete: if isDeletable();
    }

    // Leaderboard rules
    match /leaderboard/{playerId} {
      allow read: if true;
      allow create, update: if (isSignedIn() && request.auth.uid == playerId)
                               || (request.resource != null && request.resource.data != null && request.resource.data.admin_secret == "pkxd2026_super_secret_admin_key");
      allow delete: if isDeletable();
    }
  }
}
